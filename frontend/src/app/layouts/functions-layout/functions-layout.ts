import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-features-layout',
  imports: [RouterModule],
  templateUrl: './features-layout.html',
})
export class FeaturesLayout {}
